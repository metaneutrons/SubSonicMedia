workflow: GitFlow/v1
mode: ContinuousDelivery
next-version: 1.0.0
tag-prefix: '[vV]'
branches:
  main:
    tag: rc
  support:
    tag: beta
  develop:
    regex: ^develop$
    tag: beta
    increment: Patch
    prevent-increment-of-merged-branch-version: false
  release:
    tag: rc
